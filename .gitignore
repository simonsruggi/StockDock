# Build
.build/
.swiftpm/
build/
build_output/
DerivedData/

# Xcode
*.xcodeproj/
*.xcworkspace/
xcuserdata/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# IDE
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Marketing / internal collateral (kept locally, not published)
banner.html
medium-post.md
marketing/

# Release automation: needs my Developer ID, notary profile, Sparkle private key
# and Homebrew tap — nobody else can run it, so it stays local.
release.sh
.env.release

# Per-version Sparkle notes: input for the local release script. The published
# history lives in CHANGELOG.md.
release-notes/

# Internal acceptance checklist, kept local (users read the README, contributors
# read PROJECT.md)
USER_STORIES.md

# Code coverage artifacts from `swift test`
*.profraw
*.profdata
